# 🌐 转链接独立域名与主站分离配置教程（双域名架构）

> **场景说明**：
> - **主站域名**（例如 `app.com` 或 `tool.com`）：用于用户访问平台首页、登录注册、上传 HTML/ZIP、后台管理。
> - **转链接独立域名**（例如 `mysite.cc` 或 `link.cc`）：专门用于给用户生成的网页链接提供二级域名（如 `https://demo.mysite.cc`）。
> 
> 本架构支持主站与转链域名**完全独立分离**，既安全美观，又方便更换转链业务域名！

---

## 目录

- [一、双域名独立架构原理](#一双域名独立架构原理)
- [二、第一步：DNS 解析设置（主站与转链域名）](#二第一步dns-解析设置主站与转链域名)
- [三、第二步：宝塔面板 / Nginx 绑定多域名与反代](#三第二步宝塔面板--nginx-绑定多域名与反代)
- [四、第三步：配置 SSL 证书（HTTPS）](#四第三步配置-ssl-证书https)
- [五、第四步：秒转链接后台核心配置](#五第四步秒转链接后台核心配置)
- [六、第五步：前台效果验证与测试](#六第五步前台效果验证与测试)
- [七、常见问题排查](#七常见问题排查)

---

## 一、双域名独立架构原理

假设你的两个域名如下：
- **主站平台域名**：`platform.com`
- **转链接业务域名**：`linkhub.cc`

整个系统请求的路由分发逻辑如下：

```
                              [ 用户请求 ]
                                   │
                 ┌─────────────────┴─────────────────┐
                 ▼                                   ▼
   访问: https://platform.com           访问: https://my-page.linkhub.cc
   (或 https://platform.com/admin.html)               │
                 │                                   │
                 ▼                                   ▼
        [ DNS 解析至服务器 IP ]             [ DNS 泛解析 *.linkhub.cc 至服务器 IP ]
                 │                                   │
                 └─────────────────┬─────────────────┘
                                   ▼
                 [ Nginx 反向代理 (透传 Host: $host) ]
                                   ▼
                  [ Node.js 服务 (127.0.0.1:3000) ]
                                   │
        ┌──────────────────────────┴──────────────────────────┐
        ▼                                                     ▼
【命中非转链域名: platform.com】                     【命中转链域名: my-page.linkhub.cc】
提供主站前端首页、登录、后台管理与 API 服务             提取子域名前缀 my-page，输出用户部署的 HTML
```

---

## 二、第一步：DNS 解析设置（主站与转链域名）

分别在两个域名的 DNS 控制台（Cloudflare / 阿里云 / 腾讯云 等）添加解析记录：

### 1. 主站域名（如 `platform.com`）的解析记录

| 记录类型 | 主机记录 (Name) | 记录值 (Value) | 代理状态 (Cloudflare) | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **A 记录** | `@` | `你的服务器公网 IP` | ☁️ 开启代理 / 直连 | 主站根域名访问 |
| **A 记录** | `www` | `你的服务器公网 IP` | ☁️ 开启代理 / 直连 | （可选）www 访问 |

---

### 2. 转链接独立域名（如 `linkhub.cc`）的解析记录

| 记录类型 | 主机记录 (Name) | 记录值 (Value) | 代理状态 (Cloudflare) | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **A 记录** | `*` | `你的服务器公网 IP` | ☁️ 开启代理 / 直连 | **核心泛解析**：匹配所有子域名（如 `demo.linkhub.cc`） |
| **A 记录** | `@` | `你的服务器公网 IP` | ☁️ 开启代理 / 直连 | （可选）转链域名根地址 |

> 💡 **核心提示**：转链域名**必须添加主机记录为 `*`（星号）**的泛解析 A 记录！

---

## 三、第二步：宝塔面板 / Nginx 绑定多域名与反代

因为两个域名都指向这台服务器，你需要让 Nginx 同时接收这两个域名的访问并转发给 Node 服务。

### 方式 A：宝塔面板操作（推荐）

1. 打开宝塔面板 -> **【网站】** -> **【Node项目】**（或主站点设置）。
2. 找到本项目，点击 **【设置】** -> 进入 **【域名管理】** 选项卡。
3. 把**主站域名**和**转链泛域名**一次性全部填入：
   ```text
   platform.com
   www.platform.com
   linkhub.cc
   *.linkhub.cc
   ```
   *(请将上述域名替换为你自己的实际域名)*

4. **确认 Host 头透传（极关键）**：
   - 点击 **【反向代理】** -> **【配置文件】**（或站点配置文件）。
   - 检查反代代码块中必须包含 `proxy_set_header Host $host;`：
     ```nginx
     location / {
         proxy_pass http://127.0.0.1:3000;
         
         # 必须：将用户访问的具体子域名透传给 Node 服务识别
         proxy_set_header Host $host;
         proxy_set_header X-Real-IP $remote_addr;
         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
         proxy_set_header X-Forwarded-Proto $scheme;
         
         # WebSocket 协议支持
         proxy_http_version 1.1;
         proxy_set_header Upgrade $http_upgrade;
         proxy_set_header Connection "upgrade";
     }
     ```
5. 点击 **保存**。

---

### 方式 B：原生 Nginx 配置文件示例

如果是纯 Linux 环境，编辑 Nginx 配置文件（如 `/etc/nginx/conf.d/html_to_link.conf`）：

```nginx
server {
    listen 80;
    listen 443 ssl http2;

    # 包含主站域名与转链业务域名的通配符
    server_name platform.com www.platform.com linkhub.cc *.linkhub.cc;

    # SSL 证书配置（支持多域名证书或由 Cloudflare 代理）
    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;

    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 四、第三步：配置 SSL 证书（HTTPS）

双域名独立架构需要让**主站域名**和**转链泛域名**都支持 HTTPS：

### 推荐方案：使用 Cloudflare CDN 代理（最方便、免维护）
1. 将 `platform.com` 和 `linkhub.cc` 两个域名都接入 Cloudflare。
2. 将所有解析记录的 **Proxy status（代理状态）** 打开为 **Proxied（橙色小云朵 ☁️）**。
3. Cloudflare 会自动为 `platform.com` 签发主站证书，同时自动为 `*.linkhub.cc` 签发通配符泛域名证书，两边均全自动续期，零配置成本！

### 备选方案：宝塔面板申请证书
在宝塔站点设置 -> **【SSL】** 中，使用 DNS 验证方式申请包含 `platform.com`、`linkhub.cc`、`*.linkhub.cc` 的 SSL 证书并开启强制 HTTPS。

---

## 五、第四步：秒转链接后台核心配置

这是双域名独立架构中最关键的一步！

1. 浏览器打开**主站管理后台**：
   ```text
   https://platform.com/admin.html
   ```
2. 输入管理员密码登录后台。
3. 找到 **【平台核心服务与存储配置】** 卡片，点击 **【配置】** 按钮。
4. 找到 **【平台一级主域名配置】** 区域：
   - ⚠️ **特别注意**：**“一级主域名 (Primary Domain)”** 请填入你的**转链接独立域名**（例如 `linkhub.cc`），**不要**填写主站域名！
   - **访问协议 (Protocol)**：选择 `HTTPS (https://)`。

```text
┌────────────────────────────────────────────────────────┐
│ 平台一级主域名配置                                     │
│                                                        │
│ 一级主域名 (Primary Domain)                           │
│ [ linkhub.cc                                         ] │  <-- 填转链业务域名！
│                                                        │
│ 访问协议 (Protocol)                                   │
│ [ HTTPS (https://)                                 ▼ ] │
└────────────────────────────────────────────────────────┘
```

5. 点击 **【保存域名配置】** 即可实时生效！

---

## 六、第五步：前台效果验证与测试

现在测试完整的双域名分流效果：

### 1. 访问主站平台
- 打开 `https://platform.com`：正常显示秒转链接首页、上传文件框、卡密输入、登录注册。
- 打开 `https://platform.com/admin.html`：正常显示管理员管理后台。

### 2. 部署生成专属转链域名
1. 在主站 `https://platform.com` 上传一个 HTML 文件。
2. 自定义前缀输入 `myweb`（或留空自动生成短前缀）。
3. 填入卡密，点击 **【立即部署上线】**。
4. 部署成功后，系统生成的访问链接将自动采用转链域名：
   ```text
   https://myweb.linkhub.cc
   ```
5. 打开该链接，即可直接访问该网页的内容！

---

## 七、常见问题排查

### Q1：为什么后台一级主域名要填“转链域名”，而不是主站域名？
- **原因**：系统的路由判断逻辑是：凡是**不是**以转链域名结尾的请求（比如访问 `platform.com`），系统一律当作主站面板处理；凡是以转链域名结尾的子域名（比如 `myweb.linkhub.cc`），系统会自动截取前缀 `myweb` 并输出对应的网页。
- 因此后台填写的是“**分发给用户的生成链接域名**”。

---

### Q2：访问 `xxx.linkhub.cc` 提示 404 / 站点不存在？
1. 检查后台已部署站点列表中是否有该前缀的记录，或该网页是否已到期。
2. 检查转链域名 `linkhub.cc` 的 DNS 中是否正确添加了 **主机记录为 `*`** 的 A 记录泛解析。
3. 检查 Nginx 反代配置中是否遗漏了 `proxy_set_header Host $host;`。

---

### Q3：主站域名和转链域名可以随时更换吗？
- **可以**。
- 如果以后需要更换转链域名（例如从 `linkhub.cc` 更换为 `newlink.com`）：
  1. 给 `newlink.com` 添加 `*` 泛解析指向服务器。
  2. 在宝塔 / Nginx 域名管理中加上 `*.newlink.com`。
  3. 在管理后台将一级主域名改成 `newlink.com` 保存即可！
