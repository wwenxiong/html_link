# 秒转链接 · 完整部署与配置教程

> **HTML 网页秒转在线链接平台**：支持 HTML/ZIP 一键部署、卡密授权、个性化二级域名、Cloudflare R2 全球 CDN 加速、Clerk 用户注册登录系统。

---

## 目录

- [一、系统架构概览](#一系统架构概览)
- [二、环境准备与端口放行](#二环境准备与端口放行)
- [三、安装宝塔面板与运行环境](#三安装宝塔面板与运行环境)
- [四、上传项目代码与安装依赖](#四上传项目代码与安装依赖)
- [五、创建 Node.js 项目并启动](#五创建-nodejs-项目并启动)
- [六、域名解析与反向代理配置](#六域名解析与反向代理配置)
- [七、管理后台使用](#七管理后台使用)
- [八、配置 Clerk 用户认证（可选）](#八配置-clerk-用户认证可选)
- [九、配置 Cloudflare R2 对象存储（可选）](#九配置-cloudflare-r2-对象存储可选)
- [十、常见问题与排查](#十常见问题与排查)

---

## 一、系统架构概览

```
[ 用户浏览器 ]
      │
      ▼  HTTPS 请求 (主域名 / 子域名)
[ Cloudflare CDN / DNS 泛解析 *.yourdomain.com ]
      │
      ▼  80 / 443 端口
[ 服务器防火墙 / 安全组 ]
      │
      ▼
[ 宝塔 Nginx 反向代理 (透传 Host Header) ]
      │
      ▼  本地 3000 端口
[ Node.js (server.js) ]
      │
      ├── /data/         卡密、站点索引、配置 (JSON 数据库)
      ├── /public/_sites/ 本地网页存储
      └── Cloudflare R2   可选远程 CDN 存储
```

**核心特性**：
- HTML / ZIP 文件一键上传生成在线链接
- 卡密授权体系（3天体验 / 3个月 / 1年 / 无限通用卡）
- 用户自定义个性化二级域名（如 `my-site.yourdomain.com`）
- 站点有效期管理与卡密续期
- Clerk 用户注册登录 + 我的站点管理
- Cloudflare R2 全球 CDN 加速（可选）
- 轻量 JSON 文件数据库，无需安装 MySQL

---

## 二、环境准备与端口放行

### 服务器要求

| 项目 | 最低要求 |
|:---|:---|
| 操作系统 | CentOS 7+ / Ubuntu 18.04+ / Debian 10+ |
| 内存 | 512MB 以上 |
| Node.js | v18.x 或 v20.x (LTS) |
| 存储空间 | 10GB 以上 |

### 云服务器安全组端口放行

在登录服务器之前，先到云厂商控制台放行以下端口：

| 端口 | 协议 | 用途 | 是否必须 |
|:---|:---|:---|:---|
| `22` | TCP | SSH 远程连接 | ✅ 必须 |
| `80` | TCP | HTTP 网页访问 | ✅ 必须 |
| `443` | TCP | HTTPS 加密访问 | ✅ 必须 |
| `8888` | TCP | 宝塔面板初始端口 | ✅ 安装时必须 |
| `3000` | TCP | Node.js 测试端口 | ⚠️ 调试用，上线后可关闭 |

**操作路径**：云控制台 → 实例 → 安全组 / 防火墙 → 入站规则 → 添加规则

---

## 三、安装宝塔面板与运行环境

### 3.1 连接服务器

使用云控制台自带的 WebShell，或本地 SSH 工具（FinalShell / Xshell / PuTTY / Terminal）：

```
服务器 IP：你的公网 IP
用户名：root
密码：服务器初始密码
```

> Ubuntu 系统先执行 `sudo -i` 切换到 root 用户。

### 3.2 安装宝塔面板

**CentOS / Alibaba Cloud Linux**：
```bash
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh ed883de25
```

**Ubuntu / Debian**：
```bash
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh ed883de25
```

安装完成后终端会输出：

```
==================================================================
外网面板地址: http://xxx.xxx.xxx.xxx:8888/xxxxxx
username: xxxxxxxx
password: xxxxxxxx
==================================================================
```

> ⚠️ **务必将以上信息保存到本地记事本！**

### 3.3 登录宝塔面板并安装环境

1. 浏览器打开外网面板地址，输入账号密码登录
2. **安装 Nginx**：勾选 Nginx（推荐 1.22 或 1.24），选择极速安装
   - 本项目不需要 MySQL 和 PHP，可取消勾选节省内存
3. **安装 Node.js 版本管理器**：
   - 进入 **软件商店** → 搜索 `Node` → 安装 **Node.js版本管理器**
   - 安装完成后点击 **设置** → 在版本列表中安装 **v18.x** 或 **v20.x** (LTS)
   - 安装完成后点击 **设为默认**

### 3.4 验证 Node.js 安装

在宝塔终端中执行：

```bash
node -v
npm -v
```

应分别输出 `v18.x.x` / `v20.x.x` 和 `10.x.x` 即代表安装成功。

---

## 四、上传项目代码与安装依赖

### 4.1 上传源码

1. 点击宝塔侧边栏 **文件**
2. 进入路径 `/www/wwwroot/`
3. 点击 **新建目录**，命名为 `html_to_link`
4. 进入 `/www/wwwroot/html_to_link`
5. 将本地源码打成 ZIP 压缩包（**不要包含 `node_modules` 文件夹**）
6. 点击上方 **上传** → 上传 ZIP 并解压到当前目录

最终目录结构应如下：

```
/www/wwwroot/html_to_link/
├── server.js          # 主服务入口
├── package.json       # 依赖配置
├── .env.example       # 环境变量模板
├── public/            # 前端静态资源
│   ├── index.html     # 用户前台首页
│   ├── admin.html     # 管理后台页面
│   ├── css/
│   ├── js/
│   └── _sites/        # 本地站点存储（自动创建）
└── data/              # 数据存储（自动创建）
    ├── cdkeys.json    # 卡密数据库
    ├── sites.json     # 站点索引数据库
    └── config.json    # 运行时配置
```

### 4.2 配置环境变量

将 `.env.example` 复制为 `.env` 并编辑：

```bash
cd /www/wwwroot/html_to_link
cp .env.example .env
```

编辑 `.env` 文件，修改管理员密码（**其他配置可在后台面板中在线填写，此处留空即可**）：

```env
# 服务端口
PORT=3000

# 管理员密码（强烈建议修改为强密码）
ADMIN_PASSWORD=your_secure_password_here

# 以下配置可暂时留空，后续在管理后台中在线配置
# Cloudflare R2 对象存储（留空则使用服务器本地存储）
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_DOMAIN=

# 域名配置
PRIMARY_DOMAIN=
USE_HTTPS=true

# Clerk 用户认证（留空则不启用注册登录）
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
```

### 4.3 安装依赖

在宝塔终端中执行：

```bash
cd /www/wwwroot/html_to_link
npm config set registry https://registry.npmmirror.com
npm install
```

等待终端显示 `added xx packages` 即安装完成。

---

## 五、创建 Node.js 项目并启动

1. 点击宝塔侧边栏 **网站** → 顶部切换到 **Node项目** 选项卡
2. 点击 **添加Node项目**：

| 配置项 | 填写内容 |
|:---|:---|
| 项目目录 | `/www/wwwroot/html_to_link` |
| 项目名称 | `html_to_link` |
| 启动选项 | `server.js` |
| 项目端口 | `3000` |
| Node版本 | 选择 `v18.x` 或 `v20.x` |
| 执行用户 | `root` 或 `www` |

3. 点击 **提交**，项目状态显示绿色 **"运行中"** 即成功
4. 浏览器访问 `http://服务器IP:3000` 验证前台页面是否正常加载

---

## 六、域名解析与反向代理配置

### 6.1 DNS 解析设置

登录你的 DNS 服务商控制台（推荐使用 Cloudflare），添加以下两条 A 记录：

| 记录类型 | 主机记录 (Name) | 记录值 (Value) | 代理状态 | 说明 |
|:---|:---|:---|:---|:---|
| A | `@` | 你的服务器公网 IP | ☁️ Proxied（推荐） | 主站入口 |
| A | `*` | 你的服务器公网 IP | ☁️ Proxied（推荐） | 泛解析（支持任意子域名） |

> 💡 主机记录填 `*`（星号）是实现用户自定义子域名的关键！

### 6.2 宝塔绑定域名

1. 回到宝塔 **网站** → **Node项目**
2. 找到 `html_to_link` 项目，点击 **设置** → **域名管理**
3. 添加以下两个域名：
   ```
   yourdomain.com
   *.yourdomain.com
   ```

### 6.3 确认 Nginx 反向代理配置

在项目设置中进入 **反向代理** → **配置文件**，确保包含以下关键配置（使 Node.js 能识别用户访问的具体子域名）：

```nginx
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
```

> 如果缺少 `proxy_set_header Host $host;`，访问子域名时 Node.js 将无法识别用户访问的是哪个站点。

### 6.4 配置 HTTPS SSL 证书

**推荐方式：使用 Cloudflare 免费 CDN 泛域名证书**

1. 将域名托管到 [Cloudflare](https://dash.cloudflare.com/)
2. 确保 `@` 和 `*` 记录的代理状态为 **Proxied（橙色云朵 ☁️）**
3. 进入 Cloudflare **SSL/TLS** → **Overview**，选择 **Flexible（灵活）** 或 **Full（完全）**
4. Cloudflare 会自动签发并续期 `*.yourdomain.com` 通配符证书，无需在服务器上手动申请！

**备选方式：宝塔面板申请 Let's Encrypt 证书**

在宝塔站点设置中 → **SSL** → **Let's Encrypt** → 勾选域名 → 申请。

> ⚠️ 注意：Let's Encrypt 免费证书不支持通配符泛域名，需要使用 DNS 验证方式或改用 Cloudflare。

### 6.5 在后台激活域名模式

1. 访问 `https://yourdomain.com/admin.html`
2. 输入管理员密码登录
3. 找到 **平台核心服务与存储配置** → 点击 **配置**
4. 在弹窗的 **平台一级主域名配置** 中：
   - **一级主域名**：填入 `yourdomain.com`
   - **访问协议**：选择 `HTTPS (https://)`
5. 点击 **保存域名配置**

配置完成后，用户部署的网页将自动生成 `https://xxx.yourdomain.com` 格式的个性化子域名链接。

---

## 七、管理后台使用

访问 `https://yourdomain.com/admin.html`，输入管理员密码登录。

### 7.1 卡密管理

点击 **卡密管理** 卡片上的按钮打开卡密弹窗：

- **批量生成卡密**：选择有效时长类型（3天体验 / 3个月 / 1年 / 无限通用卡），设置生成数量，一键生成
- **卡密导出**：支持按类型导出（仅未使用的 3 天卡 / 3 个月卡 / 1 年卡 / 无限卡等），可导出纯密钥或完整台账
- **卡密清理**：支持按类型清理（清理已到期 / 已使用 / 未使用 / 按时长类型清理 / 全量清空）
- **筛选与搜索**：状态筛选（全部 / 未使用 / 使用中 / 已到期）+ 时长类型筛选 + 关键词搜索

### 7.2 站点管理

在 **已部署站点列表** 中可查看所有已部署的站点、查看存储类型、有效期状态、关联用户等，支持单站点删除。

### 7.3 系统配置

点击 **平台核心服务与存储配置** 卡片上的 **配置** 按钮，在弹窗中管理：

- **Clerk 用户认证配置**：填写公钥 / 私钥
- **Cloudflare R2 对象存储配置**：填写 Account ID / Access Key / Secret Key / Bucket / Public Domain，支持一键测试连接
- **平台一级主域名配置**：设置主域名与协议

---

## 八、配置 Clerk 用户认证（可选）

Clerk 提供开箱即用的用户注册 / 登录系统，启用后用户可管理自己名下的站点。

### 8.1 获取 Clerk API Keys

1. 登录 [Clerk Dashboard](https://dashboard.clerk.com/)
2. 点击 **Add application** → 输入应用名称 → 勾选登录方式（Email / Google / GitHub 等）→ **Create application**
3. 进入 **API Keys**（或访问 [https://dashboard.clerk.com/~/api-keys](https://dashboard.clerk.com/~/api-keys)）
4. 复制以下两个密钥：
   - **Publishable Key**：格式类似 `pk_test_...` 或 `pk_live_...`
   - **Secret Key**：格式类似 `sk_test_...` 或 `sk_live_...`

### 8.2 配置方式

**方式一：在管理后台在线配置（推荐）**

1. 登录管理后台 → 点击 **配置** → 在 **Clerk 用户认证配置** 中填入公钥和私钥 → **保存 Clerk 配置**

**方式二：在 `.env` 文件中配置**

```env
CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxx
```

保存后在宝塔 Node 项目列表中点击 **重启** 即可生效。

### 8.3 配置后的功能

- 前台首页显示 **注册 / 登录** 按钮
- 用户登录后显示 **头像组件** 和 **我的站点** 按钮
- 登录用户部署的站点自动关联用户账号
- 用户可在 **我的站点** 中查看、续期、删除自己的站点

---

## 九、配置 Cloudflare R2 对象存储（可选）

> Cloudflare R2 提供全球 CDN 加速，且免收出站流量费。每月 10GB 免费存储额度。

### 9.1 开启 R2 服务

1. 登录 [Cloudflare 控制台](https://dash.cloudflare.com/)
2. 左侧菜单点击 **R2 对象存储**
3. 首次使用点击 **Enable R2**（需绑定信用卡/PayPal 做身份验证，免费额度内不扣费）

### 9.2 创建存储桶

1. 在 R2 概述页点击 **Create bucket**
2. **Bucket Name**：输入 `my-html-sites`（只能用小写字母、数字和连字符）
3. **Location**：选择 `Automatic`
4. 点击 **Create Bucket**

### 9.3 配置公开访问域名

在存储桶 **Settings** 页面，找到 **Public Access**：

**选项 A：使用免费 r2.dev 子域名（快速上手）**

1. 找到 **R2.dev Subdomain** → 点击 **Allow Access** → 输入 `allow` 确认
2. 获得公共 URL：`https://pub-xxxxxxxx.r2.dev`

**选项 B：绑定自定义域名（推荐）**

1. 找到 **Custom Domains** → 点击 **Connect Domain**
2. 输入 Cloudflare 托管的域名，如 `cdn.yourdomain.com`
3. 点击 **Continue** → **Connect Domain**，Cloudflare 自动添加 DNS 记录

### 9.4 创建 API 密钥

1. 在 R2 概述页右侧点击 **Manage R2 API Tokens**
2. 点击 **Create API token**
3. 配置：
   - **Token name**：`html-to-link-key`
   - **Permissions**：选择 **Admin Read & Write**
   - **Bucket**：选择 `my-html-sites`
4. 点击 **Create API Token**
5. **立即复制保存** Access Key ID 和 Secret Access Key

> ⚠️ Secret Access Key 仅在创建时显示一次，关闭页面后无法再查看！

### 9.5 获取 Account ID

在 R2 概述页右侧找到 **Account ID**（32 位十六进制字符串），点击复制。

### 9.6 配置到项目

**方式一：在管理后台在线配置（推荐）**

1. 登录管理后台 → 点击 **配置**
2. 在 **Cloudflare R2 对象存储配置** 中填入五项参数：
   - Account ID
   - Access Key ID
   - Secret Access Key
   - Bucket Name（如 `my-html-sites`）
   - Public Domain（如 `https://cdn.yourdomain.com`）
3. 点击 **保存 R2 配置**
4. 点击 **测试连接**，提示成功即代表配置完成！

**方式二：在 `.env` 文件中配置**

```env
R2_ACCOUNT_ID=你的_ACCOUNT_ID
R2_ACCESS_KEY_ID=你的_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY=你的_SECRET_ACCESS_KEY
R2_BUCKET_NAME=my-html-sites
R2_PUBLIC_DOMAIN=https://cdn.yourdomain.com
```

保存后在宝塔 Node 项目中点击 **重启** 即可生效。

---

## 十、常见问题与排查

### Q1：访问子域名提示 404 / 站点不存在

- 确认该子域名对应的站点已成功部署（在后台站点列表中检查）
- 确认 Nginx 配置中有 `proxy_set_header Host $host;`

### Q2：访问子域名打开的是主站首页

Nginx 未透传 Host Header。检查反向代理配置中是否包含 `proxy_set_header Host $host;`。

### Q3：访问出现 502 Bad Gateway

Node.js 项目未启动或崩溃。在宝塔 Node 项目列表中点击 **日志** 查看错误信息。常见原因：
- 端口被占用：修改 `.env` 中的 `PORT`
- 依赖未安装：重新执行 `npm install`

### Q4：浏览器提示证书不安全

SSL 证书不支持泛域名。需使用通配符证书 `*.yourdomain.com`。推荐使用 Cloudflare CDN 代理模式，自动签发免费通配符证书。

### Q5：修改代码后如何重启生效？

- **后端代码 (`server.js`)**：在宝塔 Node 项目列表中点击 **重启**
- **前端页面 (`index.html` / `admin.html`)**：浏览器按 `Ctrl + F5` 强制刷新

### Q6：数据备份注意事项

用户数据保存在以下目录，更新版本时**切勿覆盖或删除**：

| 目录 / 文件 | 内容 |
|:---|:---|
| `/data/cdkeys.json` | 卡密数据库 |
| `/data/sites.json` | 站点索引数据库 |
| `/data/config.json` | 运行时配置（R2、域名、Clerk 等） |
| `/public/_sites/` | 本地网页文件存储 |

### Q7：如何不使用宝塔面板直接部署？

确保已安装 Node.js v18+ 和 Nginx，然后：

```bash
# 1. 进入项目目录
cd /www/wwwroot/html_to_link

# 2. 安装依赖
npm install

# 3. 配置环境变量
cp .env.example .env
nano .env  # 编辑配置

# 4. 启动服务
node server.js

# 5. 后台常驻运行（推荐使用 pm2）
npm install -g pm2
pm2 start server.js --name html_to_link
pm2 save
pm2 startup
```

Nginx 参考配置 (`/etc/nginx/conf.d/html_to_link.conf`)：

```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name yourdomain.com *.yourdomain.com;

    ssl_certificate     /path/to/your/cert.crt;
    ssl_certificate_key /path/to/your/cert.key;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

---

## 快速部署检查清单

完成部署后，对照以下清单确认所有功能正常：

- [ ] 访问 `https://yourdomain.com` 前台首页正常加载
- [ ] 访问 `https://yourdomain.com/admin.html` 后台登录正常
- [ ] 后台已配置主域名并保存
- [ ] 前台上传 HTML 文件生成链接成功
- [ ] 生成的子域名链接 `https://xxx.yourdomain.com` 可正常访问
- [ ] 卡密生成、使用、过期拦截功能正常
- [ ] （可选）Clerk 注册登录功能正常
- [ ] （可选）R2 存储连接测试通过

---

> 📝 如有疑问或需要技术支持，请提交 Issue 或联系管理员。
